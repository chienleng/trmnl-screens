<div class="layout layout--col layout--center">
  {% if ok %}
    <span class="value value--xlarge value--tnums">{{ renewable_pct }}%</span>
    <span class="label">NEM renewables</span>
  {% else %}
    <span class="value">No data</span>
  {% endif %}
</div>

<div class="title_bar">
  <span class="title">NEM Power</span>
</div>
